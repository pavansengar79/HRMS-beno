import React from 'react'
import Box from '@mui/material/Box'

const AddActions = ({ onSave }) => {
  return (
    <Box>
      {/* Placeholder AddActions (invoice) */}
      <button onClick={onSave}>Save (placeholder)</button>
    </Box>
  )
}

export default AddActions
