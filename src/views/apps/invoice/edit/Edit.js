import React from 'react'
import Box from '@mui/material/Box'

const Edit = ({ id }) => {
  return (
    <Box>
      {/* Placeholder Invoice Edit */}
      <div>Edit Invoice {id} (placeholder)</div>
    </Box>
  )
}

export default Edit
