import React from 'react'
import Box from '@mui/material/Box'

const Preview = ({ id }) => {
  return (
    <Box>
      {/* Placeholder Invoice Preview */}
      <div>Invoice Preview {id} (placeholder)</div>
    </Box>
  )
}

export default Preview
