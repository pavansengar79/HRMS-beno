import React from 'react'
import Box from '@mui/material/Box'

const PrintPage = ({ id }) => {
  return (
    <Box>
      {/* Placeholder Print Page */}
      <div>Print Invoice {id} (placeholder)</div>
    </Box>
  )
}

export default PrintPage
