import React from 'react'
import Box from '@mui/material/Box'

const HelpCenterLandingFooter = ({ children }) => (
  <Box>
    <div>{children || 'Help Center Footer (placeholder)'}</div>
  </Box>
)

export default HelpCenterLandingFooter
