import React from 'react'
import Box from '@mui/material/Box'

const DialogReferEarn = ({ open, onClose }) => {
  if (!open) return null
  return (
    <Box>
      {/* Placeholder DialogReferEarn */}
      <div>Dialog Refer & Earn (placeholder)</div>
    </Box>
  )
}

export default DialogReferEarn
