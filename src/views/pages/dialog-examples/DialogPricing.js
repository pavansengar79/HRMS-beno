import React from 'react'
import Box from '@mui/material/Box'

const DialogPricing = ({ open, onClose }) => {
  if (!open) return null
  return (
    <Box>
      {/* Placeholder DialogPricing */}
      <div>Dialog Pricing (placeholder)</div>
    </Box>
  )
}

export default DialogPricing
