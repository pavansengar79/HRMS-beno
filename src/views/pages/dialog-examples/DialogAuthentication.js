import React from 'react'
import Box from '@mui/material/Box'

const DialogAuthentication = ({ open, onClose }) => {
  if (!open) return null
  return (
    <Box>
      {/* Placeholder DialogAuthentication */}
      <div>Dialog Authentication (placeholder)</div>
    </Box>
  )
}

export default DialogAuthentication
