import React from 'react'
import Box from '@mui/material/Box'

const DialogCreateApp = ({ open, onClose }) => {
  if (!open) return null
  return (
    <Box>
      {/* Placeholder DialogCreateApp */}
      <div>Dialog Create App (placeholder)</div>
    </Box>
  )
}

export default DialogCreateApp
