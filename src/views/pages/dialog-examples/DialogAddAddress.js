import React from 'react'
import Box from '@mui/material/Box'

const DialogAddAddress = ({ open, onClose }) => {
  if (!open) return null
  return (
    <Box>
      {/* Placeholder DialogAddAddress */}
      <div>Dialog Add Address (placeholder)</div>
    </Box>
  )
}

export default DialogAddAddress
